### using zustand even though context API is being used

- zustand is used for global state management same Context API is doing the same.
- but is becoming providers hell , and I don't want to make is more chaotic.
- so for simplicity ,zustand is being used. 